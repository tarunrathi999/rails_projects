<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">The Loo Project</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-left">
        <li><a href="<%= categories_path %>"><span class="glyphicon glyphicon-list"></span> Categories</a></li>
        <li><a href="<%= loos_path %>"><span class="glyphicon glyphicon-list"></span> Broswe Loos</a></li>
        <li><a href="<%= new_loo_path %>"><span class="glyphicon glyphicon-plus"></span> Add Loo</a></li>
        <li><a href="<%= about_path %>">About</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li><a href="#">Welcome, <%= current_user.firstname %> <%= current_user.lastname %></a></li>
        <% else %>
        <li><a href="<%= new_user_registration_path %>">Sign up</a></li>
        <li><a href="<%= new_user_session_path %>">Sign in <span class="glyphicon glyphicon-log-in"></span></a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>