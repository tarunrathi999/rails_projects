<!DOCTYPE html>
<html>
<head>
  <title>TheLooProject</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="devise">
  <%= render partial: 'shared/header' %>
  
  <div class="container">

    <div class="devise-content">
      <div class="row">
        <div class="col-md-6 center-block" style="float: none; padding: 20px;">
          <div class="panel panel-warning">
            <div class="panel-body devise-body" style="padding: 20px 30px;">
              <% if flash[:notice] %>
                <div class="alert alert-success">
                  <%= flash[:notice] %>
                </div>
              <% end %>
              <% if flash[:alert] %>
                <div class="alert alert-danger">
                  <%= flash[:alert] %>
                </div>
              <% end %>
              <%= yield %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <footer class="footer text-center text-muted" style="margin-top: 30px;">
      <p>&copy; TheLooProject 2015.</p>
    </footer>

  </div>

</body>
</html>
